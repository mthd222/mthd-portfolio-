// All content lives in resume.json — edit it via the admin panel (npm run edit)
// or directly. This module just re-exports it for the components.
import data from './resume.json'

export const profile = data.profile
export const stats = data.stats
export const skillRadar = data.skillRadar
export const skillGroups = data.skillGroups
export const experience = data.experience
export const research = data.research
export const projects = data.projects
export const education = data.education
export const certifications = data.certifications
export const interests = data.interests
export const languages = data.languages
